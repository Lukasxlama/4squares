#include <riplib/riplib.h>

using namespace rip;
/*
void straight(double time) {
    auto &movement = Movement::get();
    movement.set_speed(-10, -10);
    int zeit = (time * 1000);
    util::sleep(zeit * 1000);
    movement.set_speed(0, 0);
}
void back(int time) {
    auto &movement = Movement::get();
    movement.set_speed(10, 10);
    util::sleep(time * 1000);
    movement.set_speed(0, 0);
}

void right() {
    auto &movement = Movement::get();
    movement.set_speed(10, -10);
    util::sleep(650);
    movement.set_speed(0,0);
}
void left() {
    auto &movement = Movement::get();
    movement.set_speed(-10, 10);
    util::sleep(650);
    movement.set_speed(0,0);
}
void turn_around() {
    auto &movement = Movement::get();
    movement.set_speed(10, -10);
    util::sleep(650 * 2);
    movement.set_speed(0,0);
}



void grab_astronauts(Servo(arm), Servo(klaue), Servo(arm2), Servo(finger)) {
    arm.move_to(30, 750);
    klaue.move_to(75, 750);
    arm.move_to(0, 7500);
    arm.move_to(10, 750);
    klaue.move_to(20, 750);
}
*/
